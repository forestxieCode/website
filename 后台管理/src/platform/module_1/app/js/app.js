// import { sidebarStatus } from '@/app/js/config'
// import { sidebarBox, subSidebarBox } from '@platformSale/app/js/config'

/**
 * 获取 main 的内容宽度
 * @param {Number} state - sidebarState
 * @returns {Number}
 */
export function getMainSectionWidth (state) {
  // const width = document.documentElement.clientWidth

  // return state === sidebarStatus.open
  //   ? width - sidebarBox.width - subSidebarBox.width
  //   : width - subSidebarBox.width
}
